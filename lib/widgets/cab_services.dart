import 'package:flutter/material.dart';

class CabServices extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Cab Services'),
    );
  }
}
